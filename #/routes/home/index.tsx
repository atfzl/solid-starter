import { css } from 'emotion';

function Home() {
  return (
    <div
      className={css`
        padding: 24px;
        display: flex;
        justify-content: space-between;
      `}
    >
      home
    </div>
  );
}

export default Home;
